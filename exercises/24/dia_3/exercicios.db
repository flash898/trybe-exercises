use('class');

// db.movies.find({
//   "category": {
//     $all: ["action","adventure"]
//   }
// });

db.movies.find({
  $and: [
    { "category": { $all: ["action"] } },
    { "imdbRating": { $gte: 7 } }
  ],
});
